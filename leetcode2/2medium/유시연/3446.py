class Solution:
    def sortMatrix(self, grid: List[List[int]]) -> List[List[int]]:
        n = len(grid)
        check = []
        i = 0
        j = 0

        while i <= n:
            if i == 0:
                for j in range(n)
                    check.append(grid[i][j])
              
