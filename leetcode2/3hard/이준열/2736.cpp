#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

class Solution {
public:
    vector<int> maximumSumQueries(vector<int>& nums1, vector<int>& nums2, vector<vector<int>>& queries) {
        int querLen = queries.size();
        int max
        vector<int> endResult(querLen);

        for (int i  = 0; i < querLen; i++)
        {
            int res = -1;
            // find least Index for query requirement
            for (int i = 0; i < )

            endResult[i] = res;
        }

        return endResult;
    }
};